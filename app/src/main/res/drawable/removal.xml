<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <stroke android:width="0dp" android:color="@android:color/transparent" /> <!-- No stroke (no underline) -->
    <corners android:radius="8dp" /> <!-- Rounded corners, adjust as needed -->
</shape>